# 🚀 快速测试命令

## ✅ 三种模式都已支持

- **模式1（推荐）**: 直接基于指标数据分析 - 不需要事件流 ⭐
- **模式2**: 基于事件流 + 指标数据 - 事件触发分析
- **模式3**: 基于事件流查找 - 分析突破/大变动事件

---

## 🧪 测试命令

### 测试1: 直接分析（推荐，不需要事件流）⭐

**执行命令**:
```powershell
conda activate u_taker
python run_ai_analysis.py --direct --symbol BTCUSDT
```

**预期结果**:
```
✅ Redis 连接成功: 38.147.173.111:6379/8

📊 直接基于指标数据进行分析:
   交易对: BTCUSDT
   时间戳: 1766484778038
   时间维度: 1m, 5m, 15m, 1h

🔍 获取 BTCUSDT 的多时间维度指标数据（时间点: 1766484778038）...
✅ 获取 1m 指标数据成功
✅ 获取 5m 指标数据成功
✅ 获取 15m 指标数据成功
✅ 获取 1h 指标数据成功
📊 找到 4/4 个时间维度的指标数据

📈 分析 1m 时间维度（基于指标数据）...
✅ 1m 分析完成

（显示完整的分析结果...）
```

**✅ 成功标志**: 
- 看到 `📊 直接基于指标数据进行分析`
- 看到 `✅ 获取 X 指标数据成功`（4个）
- 看到各时间维度的分析结果
- **不需要事件流！**

---

### 测试2: 基于事件流 + 指标数据

**执行命令**:
```powershell
conda activate u_taker
python run_ai_analysis.py --mode once --stream final_events --symbol BTCUSDT
```

**预期结果**:
```
✅ Redis 连接成功: 38.147.173.111:6379/8

📥 读取到事件:
   事件ID: BTCUSDT.combo.2h.rsi_kdj_combo.rsi_kdj_bearish.1766484778038
   交易对: BTCUSDT
   事件级别: 2

🔍 获取 BTCUSDT 的多时间维度指标数据（时间点: 1766484778038）...
✅ 获取 1m 指标数据成功
✅ 获取 5m 指标数据成功
✅ 获取 15m 指标数据成功
✅ 获取 1h 指标数据成功
📊 找到 4/4 个时间维度的指标数据

📈 分析 1m 时间维度（基于指标数据）...
✅ 1m 分析完成

📈 分析 5m 时间维度（基于指标数据）...
✅ 5m 分析完成

📈 分析 15m 时间维度（基于指标数据）...
✅ 15m 分析完成

📈 分析 1h 时间维度（基于指标数据）...
✅ 1h 分析完成

（显示多时间维度分析结果...）
✅ 分析结果已保存到: results/analysis_result_20251223_182243.json
```

**✅ 成功标志**: 
- 看到 `🔍 获取 ... 的多时间维度指标数据`
- 看到 `✅ 获取 X 指标数据成功`（4个）
- 看到各时间维度的分析结果
- 看到最终交易决策

---

### 测试3: 分析突破事件（使用事件流查找）

**执行命令**:
```powershell
conda activate u_taker
python run_ai_analysis.py --mode once --stream final_events --use-events --symbol BTCUSDT
```

**预期结果**:
```
✅ Redis 连接成功: 38.147.173.111:6379/8

📥 读取到事件:
   事件ID: BTCUSDT.combo.2h.rsi_kdj_combo.rsi_kdj_bearish.1766484778038
   交易对: BTCUSDT
   事件级别: 2

🔍 获取 BTCUSDT 的多时间维度事件...
✅ 找到 1m 事件: BTCUSDT.combo.1m.rsi_kdj_combo.rsi_kdj_bearish.1766484778030
✅ 找到 5m 事件: BTCUSDT.combo.5m.rsi_kdj_combo.rsi_kdj_bearish.1766484778000
⚠️  15m 时间维度未找到事件
✅ 找到 1h 事件: BTCUSDT.combo.1h.rsi_kdj_combo.rsi_kdj_bearish.1766484777000
📊 找到 3/4 个时间维度的事件

📈 分析 1m 时间维度...
✅ 1m 分析完成

（显示多时间维度分析结果...）
```

**✅ 成功标志**: 
- 看到 `🔍 获取 ... 的多时间维度事件`
- 看到 `✅ 找到 X 事件`（可能不是全部）
- 看到各时间维度的分析结果

---

## 📋 完整测试流程

### 步骤1: 确保基础服务运行

**PowerShell窗口1 - REST数据采集**:
```powershell
conda activate u_taker
python run_rest.py
```

**等待1-2分钟**，让系统生成指标数据。

**注意**: 直接分析模式不需要事件中心！

---

### 步骤2: 测试直接分析模式（推荐）⭐

**PowerShell窗口2**:
```powershell
conda activate u_taker
python run_ai_analysis.py --direct --symbol BTCUSDT
```

**验证**: 
- 看到 `📊 直接基于指标数据进行分析`
- 看到 `✅ 获取 X 指标数据成功`（4个）
- 看到完整的分析结果
- **不需要事件流！**

---

### 步骤3（可选）: 测试事件流模式

如果需要测试事件流模式，先启动事件中心：

**PowerShell窗口3 - 事件中心**:
```powershell
conda activate u_taker
python run_event_center.py
```

**等待2-3分钟**，让系统生成事件。

**PowerShell窗口4**:
```powershell
conda activate u_taker
python run_ai_analysis.py --mode once --stream final_events --symbol BTCUSDT
```

**验证**: 应该看到从事件流读取的事件，然后基于指标数据进行分析。

---

## 🎯 模式选择

### 模式1: 直接分析（推荐）⭐
- ✅ **不需要事件流** - 直接基于指标数据
- ✅ 快速验证通路
- ✅ 数据完整（4个时间维度）
- ✅ 数据同步（同一时间点）
- ✅ 最简单

**命令**: 
```powershell
python run_ai_analysis.py --direct --symbol BTCUSDT
```

---

### 模式2: 基于事件流 + 指标数据
- ✅ 事件触发分析
- ✅ 知道是什么事件触发的
- ⚠️ 需要事件流

**命令**: 
```powershell
python run_ai_analysis.py --mode once --stream final_events --symbol BTCUSDT
```

---

### 模式3: 使用事件流查找（--use-events）
- ✅ 分析突破/大变动事件
- ✅ 基于重要市场变化
- ⚠️ 可能只找到部分时间维度
- ⚠️ 需要事件流

**命令**: 
```powershell
python run_ai_analysis.py --mode once --stream final_events --use-events --symbol BTCUSDT
```

---

## 📝 常用命令

```powershell
# ========== 模式1: 直接分析（推荐）⭐ ==========
# 分析当前时间点
python run_ai_analysis.py --direct --symbol BTCUSDT

# 分析指定时间点
python run_ai_analysis.py --direct --symbol BTCUSDT --timestamp 1766484778038

# 分析其他币种
python run_ai_analysis.py --direct --symbol ETHUSDT

# ========== 模式2: 基于事件流 + 指标数据 ==========
# 单次分析
python run_ai_analysis.py --mode once --stream final_events --symbol BTCUSDT

# 持续监听
python run_ai_analysis.py --mode continuous --stream final_events --symbol BTCUSDT --interval 10

# ========== 模式3: 使用事件流查找 ==========
# 分析突破事件
python run_ai_analysis.py --mode once --stream final_events --use-events --symbol BTCUSDT
```

---

## ✅ 测试检查清单

### 直接分析模式（推荐）
- [ ] REST服务运行中（不需要事件中心）
- [ ] 直接分析测试成功（看到4个时间维度）
- [ ] 分析结果保存到文件

### 事件流模式（可选）
- [ ] REST服务运行中
- [ ] 事件中心运行中
- [ ] 事件流方案测试成功
- [ ] 分析结果保存到文件

---

## 🎉 完成！

现在你可以：
1. ✅ **直接分析**（`--direct`）- 不需要事件流，最快！
2. ✅ **基于事件流 + 指标数据** - 事件触发分析
3. ✅ **使用事件流查找**（`--use-events`）- 分析突破事件

**推荐**: 快速验证时使用 `--direct` 模式！

