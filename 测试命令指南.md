# 🧪 测试命令指南

## 📋 两种多时间维度分析方案

### 方案1: 指标数据（默认，快速验证）⭐
- **数据来源**: 直接从Redis读取指标数据
- **特点**: 快速、数据同步、适合验证通路
- **适用场景**: 快速测试、验证系统功能

### 方案2: 事件流查找（突破/大变动）
- **数据来源**: 从事件流中查找不同时间点的事件
- **特点**: 基于突破/大变动事件，更有针对性
- **适用场景**: 分析重要市场变化、交易信号

---

## 🚀 测试命令

### 测试1: 使用指标数据（默认，快速验证）

#### 1.1 单次分析 - 所有币种

```powershell
# 激活conda环境
conda activate u_taker

# 运行分析（使用指标数据，默认）
python run_ai_analysis.py --mode once --stream final_events
```

**预期结果**:
```
✅ Redis 连接成功: 38.147.173.111:6379/8

📥 读取到事件:
   事件ID: BTCUSDT.combo.2h.rsi_kdj_combo.rsi_kdj_bearish.1766484778038
   事件类型: combo.2h.rsi_kdj_combo.rsi_kdj_bearish
   交易对: BTCUSDT
   事件级别: 2

🔍 获取 BTCUSDT 的多时间维度指标数据（时间点: 1766484778038）...
✅ 获取 1m 指标数据成功
✅ 获取 5m 指标数据成功
✅ 获取 15m 指标数据成功
✅ 获取 1h 指标数据成功
📊 找到 4/4 个时间维度的指标数据

📈 分析 1m 时间维度（基于指标数据）...
✅ 1m 分析完成

📈 分析 5m 时间维度（基于指标数据）...
✅ 5m 分析完成

📈 分析 15m 时间维度（基于指标数据）...
✅ 15m 分析完成

📈 分析 1h 时间维度（基于指标数据）...
✅ 1h 分析完成

（显示多时间维度分析结果...）
```

**✅ 成功标志**: 
- 看到 `🔍 获取 ... 的多时间维度指标数据`
- 看到 `✅ 获取 X 指标数据成功`（4个时间维度）
- 看到 `📊 找到 4/4 个时间维度的指标数据`
- 看到各时间维度的分析结果

---

#### 1.2 单次分析 - 指定币种（BTCUSDT）

```powershell
python run_ai_analysis.py --mode once --stream final_events --symbol BTCUSDT
```

**预期结果**: 类似上面，但只分析BTCUSDT的事件

---

#### 1.3 单次分析 - 从历史数据开始

```powershell
python run_ai_analysis.py --mode once --stream final_events --history --symbol BTCUSDT
```

**预期结果**: 从事件流的最新一条开始分析

---

#### 1.4 持续监听模式

```powershell
python run_ai_analysis.py --mode continuous --stream final_events --interval 10 --symbol BTCUSDT
```

**预期结果**:
```
🔍 持续监听模式已启动
监听流: final_events
检查间隔: 10 秒
多时间维度分析: 启用
数据来源: 指标数据（快速验证）
币种过滤: BTCUSDT (只分析此币种)

等待新事件...
📥 读取到新事件: ...
（显示分析结果）
等待新事件...
```

---

### 测试2: 使用事件流查找（突破/大变动）

#### 2.1 单次分析 - 使用事件流

```powershell
python run_ai_analysis.py --mode once --stream final_events --use-events --symbol BTCUSDT
```

**预期结果**:
```
✅ Redis 连接成功: 38.147.173.111:6379/8

📥 读取到事件:
   事件ID: BTCUSDT.combo.2h.rsi_kdj_combo.rsi_kdj_bearish.1766484778038
   事件类型: combo.2h.rsi_kdj_combo.rsi_kdj_bearish
   交易对: BTCUSDT
   事件级别: 2

🔍 获取 BTCUSDT 的多时间维度事件...
✅ 找到 1m 事件: BTCUSDT.combo.1m.rsi_kdj_combo.rsi_kdj_bearish.1766484778030
✅ 找到 5m 事件: BTCUSDT.combo.5m.rsi_kdj_combo.rsi_kdj_bearish.1766484778000
⚠️  15m 时间维度未找到事件
✅ 找到 1h 事件: BTCUSDT.combo.1h.rsi_kdj_combo.rsi_kdj_bearish.1766484777000
📊 找到 3/4 个时间维度的事件

📈 分析 1m 时间维度...
✅ 1m 分析完成

📈 分析 5m 时间维度...
✅ 5m 分析完成

📈 分析 1h 时间维度...
✅ 1h 分析完成

（显示多时间维度分析结果...）
```

**✅ 成功标志**: 
- 看到 `🔍 获取 ... 的多时间维度事件`
- 看到 `✅ 找到 X 事件`（可能不是所有时间维度都有）
- 看到 `📊 找到 X/4 个时间维度的事件`
- 看到各时间维度的分析结果

**⚠️ 注意**: 可能只找到部分时间维度的事件（这是正常的，因为事件是异步生成的）

---

#### 2.2 持续监听 - 使用事件流

```powershell
python run_ai_analysis.py --mode continuous --stream final_events --use-events --interval 10 --symbol BTCUSDT
```

**预期结果**: 类似上面，但持续运行

---

### 测试3: 单时间维度分析（禁用多时间维度）

#### 3.1 只分析单个事件

```powershell
python run_ai_analysis.py --mode once --stream final_events --single-timeframe --symbol BTCUSDT
```

**预期结果**: 只分析触发的事件本身，不进行多时间维度分析

---

## 📊 方案选择建议

### 使用指标数据（默认）的场景

✅ **推荐用于**:
- 快速验证系统功能
- 测试AI分析通路
- 需要完整的多时间维度数据
- 需要快速响应

**命令**:
```powershell
python run_ai_analysis.py --mode once --stream final_events --symbol BTCUSDT
```

---

### 使用事件流的场景

✅ **推荐用于**:
- 分析重要的市场突破
- 分析大变动事件
- 需要基于事件信号的分析
- 生产环境（关注重要事件）

**命令**:
```powershell
python run_ai_analysis.py --mode once --stream final_events --use-events --symbol BTCUSDT
```

---

## 🔍 验证步骤

### 步骤1: 确保基础服务运行

#### 1.1 检查数据采集服务

```powershell
# 检查REST服务是否运行
# 应该看到任务触发日志
```

#### 1.2 检查事件中心

```powershell
# 检查事件中心是否运行
# 应该看到启动日志
```

#### 1.3 验证数据存在

```powershell
# 使用Python验证
python -c "import redis; r = redis.Redis(host='38.147.173.111', port=6379, password='112233Ww..', db=8, decode_responses=True); print('指标数据:', bool(r.get('indicators:binance:BTCUSDT:1m'))); print('事件流:', r.xlen('final_events'))"
```

**预期结果**:
```
指标数据: True
事件流: 15
```

---

### 步骤2: 测试指标数据方案（快速验证）

```powershell
python run_ai_analysis.py --mode once --stream final_events --symbol BTCUSDT
```

**验证点**:
- ✅ 看到 `🔍 获取 ... 的多时间维度指标数据`
- ✅ 看到 `✅ 获取 X 指标数据成功`（4个）
- ✅ 看到各时间维度的分析结果
- ✅ 看到最终交易决策

---

### 步骤3: 测试事件流方案（突破/大变动）

```powershell
python run_ai_analysis.py --mode once --stream final_events --use-events --symbol BTCUSDT
```

**验证点**:
- ✅ 看到 `🔍 获取 ... 的多时间维度事件`
- ✅ 看到 `✅ 找到 X 事件`（可能不是全部）
- ✅ 看到各时间维度的分析结果
- ✅ 看到最终交易决策

---

## 📝 完整测试流程

### 测试流程1: 快速验证通路（推荐）

```powershell
# 1. 确保服务运行
# - REST服务: python run_rest.py
# - 事件中心: python run_event_center.py

# 2. 测试指标数据方案（快速验证）
python run_ai_analysis.py --mode once --stream final_events --symbol BTCUSDT

# 预期: 快速获取4个时间维度的指标数据并分析
```

---

### 测试流程2: 分析突破事件

```powershell
# 1. 确保服务运行
# - REST服务: python run_rest.py
# - 事件中心: python run_event_center.py

# 2. 等待事件中心生成突破事件（可能需要几分钟）

# 3. 测试事件流方案（突破/大变动）
python run_ai_analysis.py --mode once --stream final_events --use-events --symbol BTCUSDT

# 预期: 基于突破事件进行多时间维度分析
```

---

## 🎯 常用命令速查

### 快速验证（指标数据）

```powershell
# 单次分析 - BTCUSDT
python run_ai_analysis.py --mode once --stream final_events --symbol BTCUSDT

# 持续监听 - BTCUSDT
python run_ai_analysis.py --mode continuous --stream final_events --symbol BTCUSDT --interval 10
```

### 分析突破事件（事件流）

```powershell
# 单次分析 - BTCUSDT
python run_ai_analysis.py --mode once --stream final_events --use-events --symbol BTCUSDT

# 持续监听 - BTCUSDT
python run_ai_analysis.py --mode continuous --stream final_events --use-events --symbol BTCUSDT --interval 10
```

### 单事件分析（禁用多时间维度）

```powershell
# 只分析单个事件
python run_ai_analysis.py --mode once --stream final_events --single-timeframe --symbol BTCUSDT
```

---

## ✅ 测试检查清单

### 指标数据方案测试

- [ ] 执行命令成功
- [ ] 看到 `🔍 获取 ... 的多时间维度指标数据`
- [ ] 看到 `✅ 获取 X 指标数据成功`（至少3个）
- [ ] 看到各时间维度的分析结果
- [ ] 看到最终交易决策
- [ ] 结果保存到 `results/` 目录

### 事件流方案测试

- [ ] 执行命令成功
- [ ] 看到 `🔍 获取 ... 的多时间维度事件`
- [ ] 看到 `✅ 找到 X 事件`（至少1个）
- [ ] 看到各时间维度的分析结果
- [ ] 看到最终交易决策
- [ ] 结果保存到 `results/` 目录

---

## 🎉 完成！

现在你可以：

1. ✅ **快速验证通路**: 使用指标数据方案（默认）
2. ✅ **分析突破事件**: 使用事件流方案（`--use-events`）
3. ✅ **灵活切换**: 根据需求选择合适方案

两个方案都已保留，可以根据场景选择使用！

