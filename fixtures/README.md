# Fixtures

Central index for contract, replay, workflow, and snapshot fixtures.

Phase-1 note:
- Existing fixture/test assets remain in module-local `text/` and `docs/` paths.
- `fixtures/index.yaml` records current authoritative fixture locations.
