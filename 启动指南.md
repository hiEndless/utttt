# BTCUSDT 价格监控系统 - 启动指南

## 项目结构说明

本项目是一个加密货币交易监控系统，包含以下模块：

1. **data_server/binance/rest_binance** - REST API 数据抓取服务（K线、持仓比例等历史数据）
2. **data_server/binance/ws_binance** - WebSocket 实时价格监控服务
3. **event_center** - 事件中心，处理技术指标和生成交易信号
4. **api** - API 服务器（可选，用于查询数据）
5. **agent_server** - 智能代理服务器（可选，用于分析和决策）

## 环境变量配置

首先，设置 Redis 环境变量（PowerShell）：

```powershell
$env:REDIS_HOST="38.147.173.111"
$env:REDIS_PORT="6379"
$env:REDIS_PASSWORD="112233Ww.."
$env:REDIS_DB="8"
```

## 启动步骤

### 步骤 1: 添加 BTCUSDT 到监控列表

在 Redis 中添加 BTCUSDT 到监控集合：

```powershell
python data_server/binance/ws_binance/utils/reids_connect.py
```

**预期结果：**
- 如果成功，会显示：`{'BTCUSDT'}` 或 `{b'BTCUSDT'}`

**或者使用 Python 脚本：**

```powershell
python -c "import redis; r = redis.Redis(host='38.147.173.111', port=6379, password='112233Ww..', db=8, decode_responses=True); r.sadd('symbol:binance', 'BTCUSDT'); print('已添加 BTCUSDT:', r.smembers('symbol:binance'))"
```

### 步骤 2: 启动 WebSocket 实时价格监控服务

这是**核心服务**，用于实时接收 BTCUSDT 价格变化：

**方法 1: 从项目根目录运行（推荐）**

```powershell
# 确保在项目根目录 D:\AI\utaker
python -m data_server.binance.ws_binance.market_ws
```

**方法 2: 设置 PYTHONPATH 后运行**

```powershell
# 设置 PYTHONPATH 环境变量（项目根目录）
$env:PYTHONPATH = "D:\AI\utaker"

# 然后进入目录运行
cd data_server/binance/ws_binance
python market_ws.py
```

**预期结果：**
- 看到连接日志：`[WS] Connecting => wss://fstream.binance.com/stream?streams=...`
- 看到订阅消息：`新增订阅: {'BTCUSDT'}`
- 实时打印价格数据：`收到: {'e': 'aggTrade', 's': 'BTCUSDT', 'p': '...', ...}`

**说明：**
- 该服务会监听 Redis 中的 `symbol:binance` 集合
- 自动订阅 BTCUSDT 的实时价格、深度和强平订单
- 价格数据会写入 Redis：`price:binance:BTCUSDT`
- 价格流数据会写入 Redis Stream：`ticks:binance:BTCUSDT`

### 步骤 3: 启动 REST API 数据抓取服务（可选）

用于获取历史 K 线数据和技术指标：

**方法 1: 使用启动脚本（推荐，最简单）**

```powershell
# 在项目根目录运行
python run_rest.py
```

**方法 2: 从子目录运行**

```powershell
cd data_server\binance\rest_binance\app
python main.py
```

**预期结果：**
- 看到任务触发日志：`task_trigger name=spider_1m interval=1m time=...`
- 定期抓取不同时间周期的 K 线数据（1m, 5m, 15m, 30m, 1h, 2h, 4h, 1d）
- 数据存储在 Redis 中：`klines:binance:BTCUSDT:1m` 等

**预期结果：**
- 看到任务触发日志：`task_trigger name=spider_1m interval=1m time=...`
- 定期抓取不同时间周期的 K 线数据（1m, 5m, 15m, 30m, 1h, 2h, 4h, 1d）
- 数据存储在 Redis 中：`klines:binance:BTCUSDT:1m` 等

**说明：**
- 该服务会自动监控 Redis 中的 `symbol:binance` 集合
- 当检测到 BTCUSDT 时，会自动启动数据抓取任务
- 抓取的数据包括：K线、多空比例、持仓比例、24小时行情、资金费率等

### 步骤 4: 启动事件中心（可选）

用于处理技术指标和生成交易信号：

```powershell
cd event_center
python main.py
```

**预期结果：**
- 服务启动后会在后台运行
- 定期处理指标数据并生成事件
- 事件会写入 Redis Streams

**说明：**
- 该服务会读取 Redis 中的指标数据
- 生成各种技术指标组合事件
- 可用于交易信号生成

### 步骤 5: 启动 API 服务器（可选）

用于通过 HTTP API 查询数据：

```powershell
cd api
python main.py
```

**预期结果：**
- 看到服务启动日志：`Uvicorn running on http://0.0.0.0:8000`
- 可以通过浏览器访问：`http://localhost:8000/docs` 查看 API 文档

## 查看价格数据

### 方法 1: 使用 Redis CLI

```powershell
# 查看最新价格（Hash 类型）
redis-cli -h 38.147.173.111 -p 6379 -a "112233Ww.." -n 8 HGETALL price:binance:BTCUSDT

# 查看价格流（Stream 类型，最新 10 条）
redis-cli -h 38.147.173.111 -p 6379 -a "112233Ww.." -n 8 XREVRANGE ticks:binance:BTCUSDT + - COUNT 10
```

### 方法 2: 使用 Python 脚本

```python
import redis
r = redis.Redis(host='38.147.173.111', port=6379, password='112233Ww..', db=8, decode_responses=True)

# 获取最新价格
price_data = r.hgetall('price:binance:BTCUSDT')
print(f"最新价格: {price_data.get('price')} USDT")
print(f"时间戳: {price_data.get('ts')}")

# 获取价格流（最新 5 条）
stream_data = r.xrevrange('ticks:binance:BTCUSDT', count=5)
for msg_id, fields in stream_data:
    print(f"价格: {fields['price']}, 时间: {fields['ts']}")
```

### 方法 3: 通过 API 查询（如果启动了 API 服务器）

访问：`http://localhost:8000/api/indicators/price/BTCUSDT`

## 重要提示

1. **必须启动的服务：**
   - WebSocket 服务（`market_ws.py`）- 用于实时价格监控

2. **可选服务：**
   - REST 服务（`rest_binance/app/main.py`）- 用于历史数据
   - 事件中心（`event_center/main.py`）- 用于技术分析
   - API 服务器（`api/main.py`）- 用于数据查询

3. **Redis 键说明：**
   - `symbol:binance` - 监控的交易对集合
   - `price:binance:BTCUSDT` - 最新价格（Hash）
   - `ticks:binance:BTCUSDT` - 价格流（Stream）
   - `klines:binance:BTCUSDT:1m` - K线数据（JSON）
   - `indicators:binance:BTCUSDT:1m` - 技术指标数据

4. **停止服务：**
   - 按 `Ctrl+C` 停止当前服务
   - 或者从 Redis 中移除交易对：`SREM symbol:binance BTCUSDT`

## 故障排查

1. **连接 Redis 失败：**
   - 检查环境变量是否正确设置
   - 检查 Redis 服务器是否可访问
   - 检查密码和端口是否正确

2. **WebSocket 连接失败：**
   - 检查网络连接
   - 检查 Binance API 是否可访问

3. **没有收到价格数据：**
   - 确认 BTCUSDT 已添加到 `symbol:binance` 集合
   - 检查 WebSocket 服务是否正常运行
   - 查看日志中的错误信息

