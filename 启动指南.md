# BTCUSDT 价格监控系统 - 完整启动指南

## 📋 项目结构说明

本项目是一个加密货币交易监控系统，包含以下模块：

1. **data_server/binance/rest_binance** - REST API 数据抓取服务（K线、持仓比例等历史数据）
2. **data_server/binance/ws_binance** - WebSocket 实时价格监控服务
3. **event_center** - 事件中心，处理技术指标和生成交易信号
4. **api** - API 服务器（可选，用于查询数据）
5. **agent_server** - 智能代理服务器（用于AI分析和决策）

---

## 🔧 环境变量配置

### 方法1: 使用Conda环境（推荐）

#### 1.1 在Conda环境中设置环境变量（永久设置）

**首次设置**（在激活环境后执行）：

**macOS/Linux (bash/zsh)**:
```bash
conda activate u_taker
conda env config vars set REDIS_HOST="38.147.173.111"
conda env config vars set REDIS_PORT="6379"
conda env config vars set REDIS_PASSWORD="112233Ww.."
conda env config vars set REDIS_DB="8"
```

**Windows (PowerShell)**:
```powershell
conda activate u_taker
conda env config vars set REDIS_HOST="38.147.173.111"
conda env config vars set REDIS_PORT="6379"
conda env config vars set REDIS_PASSWORD="112233Ww.."
conda env config vars set REDIS_DB="8"
```

**或者一次性设置所有变量**：

**macOS/Linux (bash/zsh)**:
```bash
conda activate u_taker
conda env config vars set REDIS_HOST="38.147.173.111" REDIS_PORT="6379" REDIS_PASSWORD="112233Ww.." REDIS_DB="8"
```

**Windows (PowerShell)**:
```powershell
conda activate u_taker
conda env config vars set REDIS_HOST="38.147.173.111" REDIS_PORT="6379" REDIS_PASSWORD="112233Ww.." REDIS_DB="8"
```

**⚠️ 重要**: 设置完成后，需要**重新激活环境**才能生效：
```bash
conda deactivate
conda activate u_taker
```

#### 1.2 查看已设置的环境变量

```bash
conda env config vars list
```

#### 1.3 删除环境变量（如需要）

```bash
conda env config vars unset REDIS_HOST
```

#### 1.4 激活环境（环境变量会自动加载）

```bash
conda activate u_taker
```

### 方法2: 手动设置环境变量

在PowerShell中设置：

```powershell
$env:REDIS_HOST="38.147.173.111"
$env:REDIS_PORT="6379"
$env:REDIS_PASSWORD="112233Ww.."
$env:REDIS_DB="8"
```

**验证环境变量**:
```powershell
echo $env:REDIS_HOST
echo $env:REDIS_PORT
echo $env:REDIS_DB
```

**预期结果**:
```
38.147.173.111
6379
8
```

---

## 🚀 完整启动流程

### 步骤 1: 添加交易对到监控列表

#### 1.1 添加BTCUSDT

**执行命令**:
```powershell
python add_symbol.py add BTCUSDT
python add_symbol.py add RIVERUSDT
#### 上升趋势
python add_symbol.py add ZKPUSDT
#### 下降趋势
python add_symbol.py add POWERUSDT

python add_symbol.py add ONTUSDT
python add_symbol.py add RVVUSDT



```

**预期结果**:
```
✓ Redis 连接成功: 38.147.173.111:6379/8
✓ 已添加 BTCUSDT 到监控列表
✓ 当前监控的交易对: ['BTCUSDT']
```

**✅ 成功标志**: 
- 看到 `✓ Redis 连接成功`
- 看到 `✓ 已添加 BTCUSDT 到监控列表`
- 看到 `✓ 当前监控的交易对: ['BTCUSDT']`

---

#### 1.2 查看当前监控的交易对（可选）

**执行命令**:
```powershell
python add_symbol.py list
```

**预期结果**:
```
✓ 当前监控的交易对: ['BTCUSDT']
```

---

#### 1.3 移除交易对（如果需要）

**执行命令**:
```powershell
python add_symbol.py remove BTCUSDT
```

**预期结果**:
```
✓ 已移除 BTCUSDT 从监控列表
✓ 当前监控的交易对: []
```

---

### 步骤 2: 启动 WebSocket 实时价格监控服务

这是**核心服务**，用于实时接收 BTCUSDT 价格变化。

**执行命令**:
```powershell
python run_ws.py
```

**预期结果**:
```
[WS] Connecting => wss://fstream.binance.com/stream?streams=...
[WS] Connected successfully
新增订阅: {'BTCUSDT'}
收到: {'e': 'aggTrade', 's': 'BTCUSDT', 'p': '43250.50', 'q': '0.001', 'T': 1703123456789, ...}
收到: {'e': 'depthUpdate', 's': 'BTCUSDT', ...}
收到: {'e': 'forceOrder', 's': 'BTCUSDT', ...}
```

**✅ 成功标志**: 
- 看到 `[WS] Connected successfully`
- 看到 `新增订阅: {'BTCUSDT'}`
- 持续收到价格数据（`aggTrade`、`depthUpdate`、`forceOrder`）

**说明**:
- 该服务会监听 Redis 中的 `symbol:binance` 集合
- 自动订阅 BTCUSDT 的实时价格、深度和强平订单
- 价格数据会写入 Redis：`price:binance:BTCUSDT`
- 价格流数据会写入 Redis Stream：`ticks:binance:BTCUSDT`

**⚠️ 重要**: 保持此窗口运行，不要关闭！

---

### 步骤 3: 启动 REST API 数据抓取服务

用于获取历史 K 线数据和技术指标。

**打开新的PowerShell窗口**，执行：

```powershell
# 如果使用conda环境
conda activate u_taker

# 设置环境变量（如果未使用conda）
$env:REDIS_HOST="38.147.173.111"
$env:REDIS_PORT="6379"
$env:REDIS_PASSWORD="112233Ww.."
$env:REDIS_DB="8"

# 启动服务
python run_rest.py
```

**预期结果**:
```
INFO:__main__:task_trigger name=spider_1m interval=1m time=2024-01-15 10:00:00
INFO:__main__:task_trigger name=spider_5m interval=5m time=2024-01-15 10:00:00
INFO:__main__:task_trigger name=spider_15m interval=15m time=2024-01-15 10:00:00
INFO:__main__:task_trigger name=spider_30m interval=30m time=2024-01-15 10:00:00
INFO:__main__:task_trigger name=spider_1h interval=1h time=2024-01-15 10:00:00
INFO:__main__:task_trigger name=spider_2h interval=2h time=2024-01-15 10:00:00
INFO:__main__:task_trigger name=spider_4h interval=4h time=2024-01-15 10:00:00
INFO:__main__:task_trigger name=spider_1d interval=1d time=2024-01-15 10:00:00
INFO:__main__:task_trigger name=ticker24hr interval=1h time=2024-01-15 10:00:00
INFO:__main__:task_trigger name=fundingRate interval=4h time=2024-01-15 10:00:00
```

然后会持续输出日志，每隔一段时间（根据interval）会有新的日志。

**✅ 成功标志**: 
- 看到多个任务的启动日志
- 日志持续输出（说明正在运行）
- 没有报错信息

**说明**:
- 该服务会自动监控 Redis 中的 `symbol:binance` 集合
- 当检测到 BTCUSDT 时，会自动启动数据抓取任务
- 抓取的数据包括：K线、多空比例、持仓比例、24小时行情、资金费率等
- 数据存储在 Redis 中：`klines:binance:BTCUSDT:1m` 等
- 技术指标存储在：`indicators:binance:BTCUSDT:1m` 等

**⚠️ 重要**: 保持此窗口运行，不要关闭！

---

### 步骤 4: 启动事件处理中心

用于处理技术指标和生成交易信号。

**打开新的PowerShell窗口**，执行：

```powershell
# 如果使用conda环境
conda activate u_taker

# 设置环境变量（如果未使用conda）
$env:REDIS_HOST="38.147.173.111"
$env:REDIS_PORT="6379"
$env:REDIS_PASSWORD="112233Ww.."
$env:REDIS_DB="8"

# 启动服务
python run_event_center.py
```

**预期结果**:
```
[IndicatorsScheduler] 开始扫描指标...
[AlertsConsumer] 开始监听告警流...
[ForceStatsConsumer] 开始监听大单统计流...
```

然后程序会持续运行，可能没有太多输出（这是正常的，事件生成是后台进行的）。

**✅ 成功标志**: 
- 看到启动日志
- 程序持续运行（不会退出）
- 没有报错信息

**说明**:
- 该服务会读取 Redis 中的指标数据
- 生成各种技术指标组合事件
- 事件会写入 Redis Streams：
  - `raw_event_stream` - 原始事件流
  - `l0_events` - L0级别事件（基础信号）
  - `l1_events` - L1级别事件（中等强度）
  - `final_events` - 最终事件流（重要事件，level >= 4）

**⚠️ 重要**: 保持此窗口运行，不要关闭！

**等待时间**: 等待2-3分钟，让事件中心生成一些事件。

---

### 步骤 5: 运行 AI 分析

用于从事件中心读取事件，调用 Agent 系统进行智能分析。

**打开新的PowerShell窗口**，执行：

#### 5.1 运行一次分析（推荐，用于测试）

**执行命令**:

##### 方案A: 使用指标数据（默认，快速验证）⭐

```powershell
# 如果使用conda环境
conda activate u_taker

# 设置环境变量（如果未使用conda）
$env:REDIS_HOST="38.147.173.111"
$env:REDIS_PORT="6379"
$env:REDIS_PASSWORD="112233Ww.."
$env:REDIS_DB="8"

# 运行一次分析（使用指标数据，默认方案）
python run_ai_analysis.py --mode once --stream final_events

# 只分析指定币种（例如：只分析 BTCUSDT）
python run_ai_analysis.py --mode once --stream final_events --symbol BTCUSDT

# 从历史数据开始分析
python run_ai_analysis.py --mode once --stream final_events --history --symbol BTCUSDT
```

**预期结果**:
```
✅ Redis 连接成功: 38.147.173.111:6379/8

📥 读取到事件:
   事件ID: BTCUSDT.combo.2h.rsi_kdj_combo.rsi_kdj_bearish.1766484778038
   事件类型: combo.2h.rsi_kdj_combo.rsi_kdj_bearish
   交易对: BTCUSDT
   事件级别: 2

🔍 获取 BTCUSDT 的多时间维度指标数据（时间点: 1766484778038）...
✅ 获取 1m 指标数据成功
✅ 获取 5m 指标数据成功
✅ 获取 15m 指标数据成功
✅ 获取 1h 指标数据成功
📊 找到 4/4 个时间维度的指标数据

📈 分析 1m 时间维度（基于指标数据）...
✅ 1m 分析完成

📈 分析 5m 时间维度（基于指标数据）...
✅ 5m 分析完成

📈 分析 15m 时间维度（基于指标数据）...
✅ 15m 分析完成

📈 分析 1h 时间维度（基于指标数据）...
✅ 1h 分析完成

（显示多时间维度分析结果...）
```

**✅ 成功标志**: 
- 看到 `🔍 获取 ... 的多时间维度指标数据`
- 看到 `✅ 获取 X 指标数据成功`（4个时间维度）
- 看到 `📊 找到 4/4 个时间维度的指标数据`
- 看到各时间维度的分析结果

---

##### 方案B: 使用事件流查找（突破/大变动）

```powershell
# 使用事件流查找多时间维度事件（适合分析突破/大变动）
python run_ai_analysis.py --mode once --stream final_events --use-events --symbol BTCUSDT
```

**预期结果**:
```
✅ Redis 连接成功: 38.147.173.111:6379/8

📥 读取到事件:
   事件ID: BTCUSDT.combo.2h.rsi_kdj_combo.rsi_kdj_bearish.1766484778038
   事件类型: combo.2h.rsi_kdj_combo.rsi_kdj_bearish
   交易对: BTCUSDT
   事件级别: 2

🔍 获取 BTCUSDT 的多时间维度事件...
✅ 找到 1m 事件: BTCUSDT.combo.1m.rsi_kdj_combo.rsi_kdj_bearish.1766484778030
✅ 找到 5m 事件: BTCUSDT.combo.5m.rsi_kdj_combo.rsi_kdj_bearish.1766484778000
⚠️  15m 时间维度未找到事件
✅ 找到 1h 事件: BTCUSDT.combo.1h.rsi_kdj_combo.rsi_kdj_bearish.1766484777000
📊 找到 3/4 个时间维度的事件

📈 分析 1m 时间维度...
✅ 1m 分析完成

（显示多时间维度分析结果...）
```

**✅ 成功标志**: 
- 看到 `🔍 获取 ... 的多时间维度事件`
- 看到 `✅ 找到 X 事件`（可能不是所有时间维度都有）
- 看到各时间维度的分析结果

**说明**: 
- 事件流方案基于突破/大变动事件，更有针对性
- 可能只找到部分时间维度的事件（这是正常的）

**预期结果**:
```
✅ Redis 连接成功: 38.147.173.111:6379/8

📥 读取到事件:
   事件ID: BTCUSDT.combo.15m.rsi_kdj_combo.rsi_kdj_bullish.1766112469336
   事件类型: combo.15m.rsi_kdj_combo.rsi_kdj_bullish
   交易对: BTCUSDT
   事件级别: 2

============================================================
开始 AI 分析...
事件类型: market_signal
事件强度: low
交易对: BTCUSDT
============================================================

================================================================================
AI 分析结果 - 2025-01-15 10:00:00
================================================================================

参与分析的 Agent: technical, risk

────────────────────────────────────────────────────────────────────────────────
Agent: technical
────────────────────────────────────────────────────────────────────────────────
{
  "agent": "technical",
  "task": "analysis",
  "content": {
    "summary": "技术指标显示看涨信号...",
    "details": "..."
  },
  "confidence": 0.75,
  "rationale": "RSI和KDJ指标均显示超买状态...",
  "metrics": {
    "auto_score": 0.85
  },
  "sources": [],
  "tool_calls": [],
  "timestamp": "2025-01-15T10:00:00"
}

────────────────────────────────────────────────────────────────────────────────
Agent: risk
────────────────────────────────────────────────────────────────────────────────
{
  "agent": "risk",
  "task": "analysis",
  "content": {
    "summary": "风险评估显示中等风险...",
    "details": "..."
  },
  "confidence": 0.65,
  "rationale": "当前市场波动率适中...",
  "metrics": {
    "auto_score": 0.72
  },
  "sources": [],
  "tool_calls": [],
  "timestamp": "2025-01-15T10:00:00"
}

────────────────────────────────────────────────────────────────────────────────
评分和权重
────────────────────────────────────────────────────────────────────────────────
自动评分: {0: 0.85, 1: 0.72}
权重分布: {'technical': 0.4, 'risk': 0.6}

────────────────────────────────────────────────────────────────────────────────
反思结果
────────────────────────────────────────────────────────────────────────────────
{
  "mode": "default",
  "reflection_scores": {
    "technical": 0.9,
    "risk": 0.7
  },
  "notes": []
}

────────────────────────────────────────────────────────────────────────────────
融合结果
────────────────────────────────────────────────────────────────────────────────
[technical:0.4] {"agent": "technical", ...}
[risk:0.6] {"agent": "risk", ...}

✅ 分析结果已保存到: results/analysis_result_20250115_100000.json
```

**✅ 成功标志**: 
- 看到 `✅ Redis 连接成功`
- 看到 `📥 读取到事件`
- 看到 `开始 AI 分析...`
- 看到多个Agent的分析结果
- 看到评分、权重、融合结果
- 看到 `✅ 分析结果已保存到: results/...`

---

#### 5.2 只分析指定币种（币种过滤）

**功能说明**:
- 使用 `--symbol` 参数可以只分析指定的币种
- 币种名称会自动转换为大写（如 `btcusdt` 会自动转换为 `BTCUSDT`）
- 在历史模式下，会自动跳过不匹配的币种，直到找到匹配的币种
- 在实时模式下，只处理匹配币种的事件

**执行命令**:
```powershell
# 只分析 BTCUSDT
python run_ai_analysis.py --mode once --stream final_events --symbol BTCUSDT

# 只分析 ETHUSDT
python run_ai_analysis.py --mode once --stream final_events --symbol ETHUSDT

# 只分析 1000SATSUSDT（持续模式）
python run_ai_analysis.py --mode continuous --stream final_events --symbol 1000SATSUSDT --interval 10

# 只分析指定币种（从历史数据开始）
python run_ai_analysis.py --mode once --stream final_events --history --symbol BTCUSDT
```

**预期结果**:
```
✅ Redis 连接成功: 38.147.173.111:6379/8

📥 读取到事件:
   事件ID: BTCUSDT.combo.15m.rsi_kdj_combo.rsi_kdj_bullish.1766112469336
   事件类型: combo.15m.rsi_kdj_combo.rsi_kdj_bullish
   交易对: BTCUSDT
   事件级别: 2
   🔍 币种过滤: BTCUSDT

（后续分析结果...）
```

**✅ 成功标志**: 
- 看到 `🔍 币种过滤: [币种名称]`
- 只处理指定币种的事件
- 其他币种的事件会被自动跳过

---

#### 5.3 持续运行模式（用于生产环境）

**执行命令**:
```powershell
# 持续监听事件流，每10秒检查一次
python run_ai_analysis.py --mode continuous --stream final_events --interval 10

# 持续监听并只分析指定币种
python run_ai_analysis.py --mode continuous --stream final_events --interval 10 --symbol BTCUSDT
```

**预期结果**:
```
✅ Redis 连接成功: 38.147.173.111:6379/8

🔍 持续监听模式已启动
事件流: final_events
检查间隔: 10 秒

等待新事件...
📥 读取到新事件: ...
（显示分析结果）
等待新事件...
📥 读取到新事件: ...
（显示分析结果）
...
```

**✅ 成功标志**: 
- 看到 `🔍 持续监听模式已启动`
- 持续输出新事件的分析结果
- 程序持续运行

**说明**:
- 该模式会持续监听事件流
- 一旦有新事件就会自动分析
- 适合生产环境使用
- 可以结合 `--symbol` 参数只监听特定币种

---

#### 5.5 命令行参数说明

**所有可用参数**:

| 参数 | 说明 | 示例 |
|------|------|------|
| `--mode` | 运行模式：`once`（运行一次）或 `continuous`（持续运行） | `--mode once` |
| `--stream` | 要监听的事件流：`raw_event_stream`, `l0_events`, `l1_events`, `final_events` | `--stream final_events` |
| `--interval` | 持续模式下的检查间隔（秒） | `--interval 10` |
| `--history` | 读取历史数据（从最新的一条开始） | `--history` |
| `--single-timeframe` | 禁用多时间维度分析，使用单一事件分析 | `--single-timeframe` |
| `--use-events` | 使用事件流查找多时间维度（默认使用指标数据），适合分析突破/大变动事件 | `--use-events` |
| `--symbol` | 只分析指定币种（如：BTCUSDT, ETHUSDT） | `--symbol BTCUSDT` |

**常用命令组合**:

```powershell
# ========== 方案A: 使用指标数据（默认，快速验证） ==========

# 1. 分析所有币种（单次，使用指标数据）
python run_ai_analysis.py --mode once --stream final_events

# 2. 只分析 BTCUSDT（单次，使用指标数据）
python run_ai_analysis.py --mode once --stream final_events --symbol BTCUSDT

# 3. 只分析 BTCUSDT（从历史数据开始，使用指标数据）
python run_ai_analysis.py --mode once --stream final_events --history --symbol BTCUSDT

# 4. 持续监听指定币种（使用指标数据）
python run_ai_analysis.py --mode continuous --stream final_events --interval 10 --symbol BTCUSDT

# ========== 方案B: 使用事件流查找（突破/大变动） ==========

# 5. 只分析 BTCUSDT（单次，使用事件流）
python run_ai_analysis.py --mode once --stream final_events --use-events --symbol BTCUSDT

# 6. 持续监听指定币种（使用事件流）
python run_ai_analysis.py --mode continuous --stream final_events --use-events --interval 10 --symbol BTCUSDT

# ========== 其他选项 ==========

# 7. 使用单时间维度分析（禁用多时间维度）
python run_ai_analysis.py --mode once --stream final_events --single-timeframe --symbol BTCUSDT

# 8. 查看帮助信息
python run_ai_analysis.py --help
```

**方案选择**:
- **指标数据（默认）**: 快速验证、测试通路、需要完整数据
- **事件流（--use-events）**: 分析突破/大变动、关注重要事件

---

#### 5.4 监听不同的事件流

**监听原始事件流**（所有事件）:
```powershell
python run_ai_analysis.py --mode once --stream raw_event_stream
```

**监听L0级别事件**:
```powershell
python run_ai_analysis.py --mode once --stream l0_events
```

**监听L1级别事件**:
```powershell
python run_ai_analysis.py --mode once --stream l1_events
```

**监听最终事件流**（推荐，只分析重要事件）:
```powershell
python run_ai_analysis.py --mode once --stream final_events
```

---

### 步骤 6: 启动 API 服务器（可选）

用于通过 HTTP API 查询数据。

**打开新的PowerShell窗口**，执行：

```powershell
# 如果使用conda环境
conda activate u_taker

# 设置环境变量（如果未使用conda）
$env:REDIS_HOST="38.147.173.111"
$env:REDIS_PORT="6379"
$env:REDIS_PASSWORD="112233Ww.."
$env:REDIS_DB="8"

# 启动服务
cd api
python main.py
```

**预期结果**:
```
INFO:     Started server process [12345]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

**✅ 成功标志**: 
- 看到服务器启动信息
- 显示运行在 `http://0.0.0.0:8000`

**测试API**:
在浏览器访问：
- API文档: `http://localhost:8000/docs`
- 查看监控的交易对: `http://localhost:8000/api/indicators/symbols?exchange=binance`
- 查看价格: `http://localhost:8000/api/indicators/price/BTCUSDT`

---

## 📊 查看数据

### 方法 1: 使用 Redis CLI

```powershell
# 查看最新价格（Hash 类型）
redis-cli -h 38.147.173.111 -p 6379 -a "112233Ww.." -n 8 HGETALL price:binance:BTCUSDT

# 查看价格流（Stream 类型，最新 10 条）
redis-cli -h 38.147.173.111 -p 6379 -a "112233Ww.." -n 8 XREVRANGE ticks:binance:BTCUSDT + - COUNT 10

# 查看事件流（最新 10 条）
redis-cli -h 38.147.173.111 -p 6379 -a "112233Ww.." -n 8 XREVRANGE final_events + - COUNT 10
```

### 方法 2: 使用 Python 脚本

```python
import redis
r = redis.Redis(host='38.147.173.111', port=6379, password='112233Ww..', db=8, decode_responses=True)

# 获取最新价格
price_data = r.hgetall('price:binance:BTCUSDT')
print(f"最新价格: {price_data.get('price')} USDT")
print(f"时间戳: {price_data.get('ts')}")

# 获取价格流（最新 5 条）
stream_data = r.xrevrange('ticks:binance:BTCUSDT', count=5)
for msg_id, fields in stream_data:
    print(f"价格: {fields['price']}, 时间: {fields['ts']}")

# 获取事件流（最新 5 条）
event_data = r.xrevrange('final_events', count=5)
for msg_id, fields in event_data:
    print(f"事件: {fields}")
```

### 方法 3: 通过 API 查询（如果启动了 API 服务器）

访问：
- `http://localhost:8000/api/indicators/price/BTCUSDT` - 查看价格
- `http://localhost:8000/api/indicators/symbols?exchange=binance` - 查看监控的交易对
- `http://localhost:8000/api/indicators/indicators?exchange=binance&symbol=BTCUSDT&interval=1h` - 查看技术指标

---

## 📋 完整启动检查清单

### ✅ 步骤1: 添加交易对
- [ ] 执行 `python add_symbol.py add BTCUSDT` 成功
- [ ] 看到 `✓ 已添加 BTCUSDT 到监控列表`

### ✅ 步骤2: WebSocket服务
- [ ] 执行 `python run_ws.py` 成功
- [ ] 看到 `新增订阅: {'BTCUSDT'}`
- [ ] 持续收到价格数据

### ✅ 步骤3: REST数据抓取服务
- [ ] 执行 `python run_rest.py` 成功
- [ ] 看到任务触发日志
- [ ] 日志持续输出

### ✅ 步骤4: 事件处理中心
- [ ] 执行 `python run_event_center.py` 成功
- [ ] 看到启动日志
- [ ] 程序持续运行

### ✅ 步骤5: AI分析
- [ ] 执行 `python run_ai_analysis.py --mode once --stream final_events` 成功
- [ ] 看到AI分析结果
- [ ] 看到分析结果保存到文件

### ✅ 步骤6: API服务（可选）
- [ ] 执行 `python main.py` 成功（在api目录）
- [ ] 看到服务启动信息
- [ ] 可以访问API接口

---

## 🎯 服务运行状态

### 必须运行的服务（核心功能）

1. **WebSocket服务** (`run_ws.py`) - 实时价格监控
2. **REST服务** (`run_rest.py`) - 历史数据和技术指标
3. **事件中心** (`run_event_center.py`) - 生成交易信号

### 可选服务

4. **AI分析** (`run_ai_analysis.py`) - 智能分析和决策
5. **API服务器** (`api/main.py`) - 数据查询接口

---

## 📝 Redis 键说明

- `symbol:binance` - 监控的交易对集合
- `price:binance:BTCUSDT` - 最新价格（Hash）
- `ticks:binance:BTCUSDT` - 价格流（Stream）
- `klines:binance:BTCUSDT:1m` - K线数据（JSON）
- `indicators:binance:BTCUSDT:1m` - 技术指标数据
- `raw_event_stream` - 原始事件流
- `l0_events` - L0级别事件流
- `l1_events` - L1级别事件流
- `final_events` - 最终事件流（重要事件）

---

## 🛑 停止服务

### 停止单个服务
- 在对应的PowerShell窗口中按 `Ctrl+C`

### 停止所有服务
- 依次在每个PowerShell窗口中按 `Ctrl+C`

### 移除交易对（停止监控）
```powershell
python add_symbol.py remove BTCUSDT
```

---

## 🔍 故障排查

### 问题1: Redis连接失败

**症状**: 各种服务报错 `Redis 连接失败`

**解决方案**:
- 检查环境变量是否正确设置
- 检查 Redis 服务器是否可访问
- 验证密码和端口是否正确
- 测试连接: `python add_symbol.py list`

---

### 问题2: WebSocket连接失败

**症状**: `[WS] Connection failed`

**解决方案**:
- 检查网络连接
- 检查 Binance API 是否可访问
- 查看详细错误信息

---

### 问题3: 没有收到价格数据

**症状**: WebSocket服务运行但没有收到数据

**解决方案**:
- 确认 BTCUSDT 已添加到 `symbol:binance` 集合: `python add_symbol.py list`
- 检查 WebSocket 服务是否正常运行
- 查看日志中的错误信息

---

### 问题4: 事件中心没有生成事件

**症状**: 事件流为空

**解决方案**:
- 等待更长时间（事件生成有延迟）
- 检查是否有技术指标数据（事件依赖指标数据）
- 检查 REST 服务是否正常运行
- 尝试监听 `raw_event_stream` 查看是否有原始事件

---

### 问题5: AI分析失败

**症状**: `❌ AI 分析失败`

**解决方案**:
- 检查 Agent 系统配置
- 检查 LLM API 密钥是否正确
- 查看详细错误信息
- 确认事件中心正在运行并产生事件

---

### 问题6: 未读取到事件

**症状**: `⚠️  未读取到新事件`

**解决方案**:
- 检查事件中心是否正在运行
- 检查指定的事件流是否有数据
- 尝试监听其他事件流（如 `raw_event_stream`）
- 等待更长时间（事件生成需要时间）

---

## 📚 相关文档

- **AI分析脚本使用说明**: `AI分析脚本使用说明.md`
- **事件监控脚本**: `monitor_events.py`
- **事件源检查脚本**: `check_event_sources.py`

---

## 🎉 完成！

如果所有步骤都成功，你的系统现在正在：

- ✅ 实时监控 BTCUSDT 价格
- ✅ 采集历史数据和技术指标
- ✅ 生成交易信号事件
- ✅ 进行AI智能分析
- ✅ 可以通过API查询数据

继续监控系统运行，查看AI分析结果！
