[pytest]
# 注册集成测试标记，避免 UnknownMarkWarning。
markers =
    integration: 依赖外部环境（如本地 Redis）的集成测试
testpaths =
    verification/validators/feature_service
    verification/validators/market_state_engine
    event_center/text
    agent_server/text
    verification/validators/execution_service
