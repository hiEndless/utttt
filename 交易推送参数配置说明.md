# 交易推送参数配置说明

本文档记录交易 JSON 推送到 Redis 的所有配置参数，方便后续融合到主程序中。

## 一、Redis 配置

### 环境变量
```bash
TRADE_REDIS_HOST=38.147.173.111     # Redis 主机地址
TRADE_REDIS_PORT=6379               # Redis 端口
TRADE_REDIS_PASSWORD=112233Ww..     # Redis 密码
TRADE_REDIS_DB=8                    # Redis 数据库编号
TRADE_TASK_KEY=TASK_ADD_TRADE       # 交易任务队列名称
```

### 配置字典结构
```python
REDIS_CONFIG = {
    'host': os.environ.get('TRADE_REDIS_HOST', '38.147.173.111'),
    'port': int(os.environ.get('TRADE_REDIS_PORT', 6379)),
    'password': os.environ.get('TRADE_REDIS_PASSWORD', '112233Ww..'),
    'db': int(os.environ.get('TRADE_REDIS_DB', 8)),
    'encoding': 'utf-8',
    'decode_responses': False  # 币安系统使用 bytes 模式
}
```

## 二、交易参数配置

### 环境变量
```bash
# 基础交易参数
TEST_SYMBOL=MYXUSDT                 # 交易对（默认: MYXUSDT）
TEST_PRICE=5.620                    # 开仓价格（默认: 5.620）
TEST_AMOUNT=100                     # 交易数量（默认: 100）
TEST_LEVERAGE=20.0                  # 杠杆倍数（默认: 20.0）

# 系统参数
TEST_TASK_ID=23                     # 任务ID（默认: 23）
TEST_USER_ID=2                      # 用户ID（默认: 2）
TEST_API_ID=0                       # API ID（默认: 0）

# 止盈止损配置
TEST_TRADE_TRIGGER_MODE=1           # 止盈止损模式：0=关闭, 1=开启（默认: 1）
TEST_TP_TRIGGER_PX=10.0             # 止盈比例（百分比），例如：10.0 表示10%（默认: 10.0）
TEST_SL_TRIGGER_PX=20.0             # 止损比例（百分比），例如：20.0 表示20%（默认: 20.0）
```

### 默认值
```python
# 基础交易参数
DEFAULT_SYMBOL = os.environ.get('TEST_SYMBOL', 'MYXUSDT')
DEFAULT_PRICE = float(os.environ.get('TEST_PRICE', '5.620'))
DEFAULT_AMOUNT = os.environ.get('TEST_AMOUNT', '100')
DEFAULT_LEVERAGE = float(os.environ.get('TEST_LEVERAGE', '20.0'))

# 系统参数
DEFAULT_TASK_ID = int(os.environ.get('TEST_TASK_ID', '23'))
DEFAULT_USER_ID = int(os.environ.get('TEST_USER_ID', '2'))
DEFAULT_API_ID = int(os.environ.get('TEST_API_ID', '0'))

# 止盈止损配置
DEFAULT_TRADE_TRIGGER_MODE = int(os.environ.get('TEST_TRADE_TRIGGER_MODE', '1'))
DEFAULT_TP_TRIGGER_PX = float(os.environ.get('TEST_TP_TRIGGER_PX', '10.0'))
DEFAULT_SL_TRIGGER_PX = float(os.environ.get('TEST_SL_TRIGGER_PX', '20.0'))
```

## 三、create_trade_json 函数参数

### 函数签名
```python
def create_trade_json(
    order_type: str,              # 订单类型 ("open", "close", "reduce")
    symbol: str,                  # 交易对 (如 "MYXUSDT")
    position_side: str,           # 持仓方向 ("LONG", "SHORT")
    side: str,                    # 交易方向 ("BUY", "SELL")
    price: float,                 # 开仓价格
    amount: str,                  # 交易数量（字符串）
    leverage: float = 5.0,        # 杠杆倍数
    task_id: int = 0,             # 任务ID
    user_id: int = 2,             # 用户ID
    api_id: int = 0,              # API ID
    trade_trigger_mode: int = 1,  # 止盈止损模式：0=关闭, 1=开启
    tp_trigger_px: float = 0.0,   # 止盈比例（百分比），例如：10.0 表示10%
    sl_trigger_px: float = 0.0,   # 止损比例（百分比），例如：20.0 表示20%
    flag: str = "1",              # 标志 ("0"=实盘, "1"=模拟盘)
    api_key: str = "",            # API Key
    api_secret: str = "",         # API Secret
    api_passphrase: str = "",     # API Passphrase
    proxies: dict = None          # 代理配置
) -> Dict[str, Any]:
```

### 参数说明

| 参数 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| `order_type` | str | 是 | 订单类型 | "open"（开仓）/"close"（平仓）/"reduce"（减仓） |
| `symbol` | str | 是 | 交易对 | "MYXUSDT" |
| `position_side` | str | 是 | 持仓方向 | "LONG"（做多）/"SHORT"（做空） |
| `side` | str | 是 | 交易方向 | "BUY"（买入）/"SELL"（卖出） |
| `price` | float | 是 | 开仓价格 | 5.620 |
| `amount` | str | 是 | 交易数量（字符串） | "100" |
| `leverage` | float | 否 | 杠杆倍数 | 20.0 |
| `task_id` | int | 否 | 任务ID | 23 |
| `user_id` | int | 否 | 用户ID | 2 |
| `api_id` | int | 否 | API ID | 0 |
| `trade_trigger_mode` | int | 否 | 止盈止损模式 | 0=关闭, 1=开启（默认: 1） |
| `tp_trigger_px` | float | 否 | 止盈比例（百分比） | 10.0 表示10% |
| `sl_trigger_px` | float | 否 | 止损比例（百分比） | 20.0 表示20% |
| `flag` | str | 否 | 标志 | "0"=实盘, "1"=模拟盘（默认: "1"） |
| `api_key` | str | 否 | API Key | "" |
| `api_secret` | str | 否 | API Secret | "" |
| `api_passphrase` | str | 否 | API Passphrase | "" |
| `proxies` | dict | 否 | 代理配置 | None |

## 四、交易 JSON 完整结构

### 字段说明

```json
{
  // ==================== 核心交易参数 ====================
  "order_type": "open",                    // 订单类型: "open"/"close"/"reduce"
  "symbol": "MYXUSDT",                     // 交易对
  "positionSide": "LONG",                  // 持仓方向: "LONG"/"SHORT"
  "side": "BUY",                           // 交易方向: "BUY"/"SELL"
  "leverage": 20.0,                        // 杠杆倍数
  "sums": "100",                           // 交易数量（字符串，已格式化）
  "openAvgPx": 5.620,                      // 开仓价格
  
  // ==================== 止盈止损配置 ====================
  "trade_trigger_mode": 1,                 // 止盈止损模式: 0=关闭, 1=开启
  "tp_trigger_px": 10.0,                   // 止盈比例（百分比），例如：10.0 表示10%
  "sl_trigger_px": 20.0,                   // 止损比例（百分比），例如：20.0 表示20%
  
  // ==================== 系统参数 ====================
  "task_id": 23,                           // 任务ID
  "user_id": 2,                            // 用户ID
  "api_id": 0,                             // API ID
  "trader_platform": 2,                    // 交易平台: 2=币安
  "flag": "1",                             // 标志: "0"=实盘, "1"=模拟盘
  
  // ==================== API 配置 ====================
  "acc": {
    "key": "",                             // API Key
    "secret": "",                          // API Secret
    "passphrase": "",                      // API Passphrase
    "proxies": {},                         // 代理配置
    "exchange": 2                          // 交易所: 2=币安
  },
  
  // ==================== 固定参数 ====================
  "follow_type": 2,
  "uniqueName": "ai_trading_system",
  "role_type": 1,
  "reduce_ratio": 0.0,
  "multiple": 1.0,
  "ratio": 0.0,
  "lever_set": 1,
  "first_order_set": 1,
  "fast_mode": 1,
  "investment": 100.0,
  "benchMark": 100.0,
  "ip_id": 1,
  "posSide_set": 1,
  "pos_mode": 0,
  "pos_value": "long",                     // "long" 或 "short"（从 position_side 自动转换）
  
  // ==================== 风控参数 ====================
  "vol24h_mode": 0,
  "vol24h_num": 10,
  "white_list_mode": 0,
  "white_list": [],
  "black_list_mode": 0,
  "black_list": [],
  "balance_monitor_mode": 0,
  "balance_monitor_value": 1000.0,
  "private_set": 0
}
```

## 五、交易类型映射

### 开多（LONG）
```python
order_type = "open"
position_side = "LONG"
side = "BUY"
trade_trigger_mode = 1  # 开启止盈止损
tp_trigger_px = 10.0    # 止盈10%
sl_trigger_px = 20.0    # 止损20%
```

### 平多（平仓 LONG）
```python
order_type = "close"
position_side = "LONG"
side = "SELL"           # 平多需要卖出
trade_trigger_mode = 1  # 可设置为 1，但 tp/sl 设为 0.0
tp_trigger_px = 0.0
sl_trigger_px = 0.0
```

### 开空（SHORT）
```python
order_type = "open"
position_side = "SHORT"
side = "SELL"
trade_trigger_mode = 1  # 开启止盈止损
tp_trigger_px = 10.0    # 止盈10%
sl_trigger_px = 20.0    # 止损20%
```

### 平空（平仓 SHORT）
```python
order_type = "close"
position_side = "SHORT"
side = "BUY"            # 平空需要买入
trade_trigger_mode = 1  # 可设置为 1，但 tp/sl 设为 0.0
tp_trigger_px = 0.0
sl_trigger_px = 0.0
```

## 六、数量格式化说明

### 币安精度要求
- 数量需要符合交易对的 `stepSize` 精度要求
- 名义价值（数量 × 价格）必须 ≥ 5 USDT（币安最小要求）
- 函数 `format_quantity()` 会自动处理精度和对齐

### 精度映射（预设值）
```python
precision_map = {
    "BTCUSDT": 0.001,
    "ETHUSDT": 0.001,
    "BNBUSDT": 0.001,
    "SOLUSDT": 0.01,
    "ADAUSDT": 0.1,
    "DOGEUSDT": 1.0,
    "BEATUSDT": 1.0,
    # 其他币种默认: 0.01
}
```

### 自动查询
函数会优先从币安 API 查询实际的 `stepSize`，查询失败时才使用预设值。

## 七、使用示例

### Python 代码示例

#### 示例1：开多（带止盈止损）
```python
trade_json = create_trade_json(
    order_type="open",
    symbol="MYXUSDT",
    position_side="LONG",
    side="BUY",
    price=5.620,
    amount="100",
    leverage=20.0,
    task_id=23,
    user_id=2,
    api_id=0,
    trade_trigger_mode=1,  # 开启止盈止损
    tp_trigger_px=10.0,    # 止盈10%
    sl_trigger_px=20.0,    # 止损20%
    flag="1"               # 模拟盘
)
```

#### 示例2：平多
```python
trade_json = create_trade_json(
    order_type="close",
    symbol="MYXUSDT",
    position_side="LONG",
    side="SELL",
    price=5.620,
    amount="100",
    leverage=20.0,
    task_id=23,
    user_id=2,
    api_id=0,
    trade_trigger_mode=1,  # 可以是1，但止盈止损设为0
    tp_trigger_px=0.0,
    sl_trigger_px=0.0,
    flag="1"
)
```

#### 示例3：推送到 Redis
```python
import redis
import json

# 连接 Redis
r = redis.Redis(
    host='38.147.173.111',
    port=6379,
    password='112233Ww..',
    db=8,
    decode_responses=False
)

# 推送交易 JSON
json_str = json.dumps(trade_json, ensure_ascii=False)
result = r.lpush('TASK_ADD_TRADE', json_str)
print(f"推送成功，队列长度: {result}")
```

## 八、注意事项

1. **数量格式化**：必须使用 `format_quantity()` 函数格式化数量，确保符合币安精度要求
2. **名义价值**：数量 × 价格必须 ≥ 5 USDT，否则会自动调整
3. **止盈止损模式**：
   - `trade_trigger_mode=1` 时，使用百分比模式
   - `tp_trigger_px` 和 `sl_trigger_px` 为百分比值，例如 10.0 表示 10%
   - 平仓时可以将 `tp_trigger_px` 和 `sl_trigger_px` 设为 0.0
4. **Redis 编码**：必须使用 `decode_responses=False`（bytes 模式），币安系统要求
5. **API 配置**：实盘交易时需要提供有效的 `api_key`、`api_secret`、`api_passphrase`
6. **测试环境**：使用 `flag="1"` 表示模拟盘，`flag="0"` 表示实盘

## 九、集成到主程序

### 1. 导入必要模块
```python
from agent_server.utils.trading_executor import TradingExecutor
# 或使用 test_push_trade_to_redis.py 中的函数
```

### 2. 配置参数来源
- 优先从环境变量读取
- 其次从配置文件读取
- 最后使用默认值

### 3. 调用流程
```python
# 1. 创建交易 JSON
trade_json = create_trade_json(...)

# 2. 推送到 Redis
success = push_to_redis(trade_json, REDIS_CONFIG, TRADE_TASK_NAME)
```

### 4. 错误处理
- 数量格式化失败：使用原始值并记录警告
- Redis 连接失败：返回 False，记录错误信息
- 队列推送失败：捕获异常并记录详细错误信息

## 十、Redis 连接和推送函数

### connect_redis 函数
```python
def connect_redis(redis_config: Dict) -> Optional[redis.Redis]:
    """
    连接 Redis
    
    Args:
        redis_config: Redis 配置字典
        
    Returns:
        Redis 客户端对象，如果连接失败则返回 None
    """
```

**配置参数：**
- `host`: Redis 主机地址
- `port`: Redis 端口
- `password`: Redis 密码
- `db`: Redis 数据库编号
- `encoding`: 编码方式（默认: 'utf-8'）
- `decode_responses`: 是否自动解码响应（必须为 False，使用 bytes 模式）
- `socket_connect_timeout`: 连接超时（默认: 5秒）
- `socket_timeout`: 套接字超时（默认: 5秒）

**错误处理：**
- `ConnectionError`: Redis 连接失败，返回 None
- 其他异常：捕获并返回 None

### push_to_redis 函数
```python
def push_to_redis(
    trade_json: Dict[str, Any], 
    redis_config: Dict,
    queue_name: str
) -> bool:
    """
    推送交易 JSON 到 Redis 队列
    
    Args:
        trade_json: 交易 JSON 字典
        redis_config: Redis 配置
        queue_name: 队列名称
        
    Returns:
        是否推送成功
    """
```

**推送方式：**
- 使用 `LPUSH` 命令（左推，后进先出）
- JSON 字符串使用 `json.dumps()` 序列化
- 自动关闭 Redis 连接

**错误处理：**
- Redis 连接失败：返回 False
- Redis 操作失败：捕获 `RedisError` 并返回 False
- 其他异常：记录详细堆栈信息并返回 False

## 十一、完整代码示例

### 完整流程示例
```python
import os
import redis
import json
from typing import Dict, Any

# 1. 配置 Redis
REDIS_CONFIG = {
    'host': os.environ.get('TRADE_REDIS_HOST', '38.147.173.111'),
    'port': int(os.environ.get('TRADE_REDIS_PORT', 6379)),
    'password': os.environ.get('TRADE_REDIS_PASSWORD', '112233Ww..'),
    'db': int(os.environ.get('TRADE_REDIS_DB', 8)),
    'encoding': 'utf-8',
    'decode_responses': False
}

TRADE_TASK_NAME = os.environ.get('TRADE_TASK_KEY', 'TASK_ADD_TRADE')

# 2. 创建交易 JSON
trade_json = {
    "order_type": "open",
    "symbol": "MYXUSDT",
    "positionSide": "LONG",
    "side": "BUY",
    "leverage": 20.0,
    "sums": "100",
    "openAvgPx": 5.620,
    "trade_trigger_mode": 1,
    "tp_trigger_px": 10.0,
    "sl_trigger_px": 20.0,
    "task_id": 23,
    "user_id": 2,
    "api_id": 0,
    "trader_platform": 2,
    "flag": "1",
    # ... 其他字段
}

# 3. 推送到 Redis
try:
    r = redis.Redis(
        host=REDIS_CONFIG['host'],
        port=REDIS_CONFIG['port'],
        password=REDIS_CONFIG['password'],
        db=REDIS_CONFIG['db'],
        decode_responses=False
    )
    r.ping()  # 测试连接
    
    json_str = json.dumps(trade_json, ensure_ascii=False)
    result = r.lpush(TRADE_TASK_NAME, json_str)
    print(f"推送成功，队列长度: {result}")
    
    r.close()
except Exception as e:
    print(f"推送失败: {e}")
```

## 十二、常见问题

### Q1: 数量格式化失败怎么办？
**A:** 函数会自动使用原始值，并输出警告信息。建议检查：
- 交易对的 stepSize 是否正确
- 数量是否符合币安最小名义价值要求（≥5 USDT）

### Q2: Redis 连接失败怎么办？
**A:** 检查：
- Redis 服务是否运行
- 主机地址、端口、密码是否正确
- 网络连接是否正常
- Redis 配置的 `decode_responses` 必须为 False

### Q3: 止盈止损不生效？
**A:** 检查：
- `trade_trigger_mode` 是否设置为 1（开启）
- `tp_trigger_px` 和 `sl_trigger_px` 是否为百分比值（不是价格值）
- 平仓操作不需要设置止盈止损（设为 0.0）

### Q4: 交易对精度如何获取？
**A:** 
- 优先从币安 API 自动查询
- 查询失败时使用预设映射表
- 可手动添加新的交易对到映射表

### Q5: 如何切换实盘/模拟盘？
**A:** 
- 设置 `flag="0"` 为实盘
- 设置 `flag="1"` 为模拟盘（默认）
- 实盘需要提供有效的 API 凭证

## 十三、最佳实践

1. **环境变量配置**：优先使用环境变量，避免硬编码敏感信息
2. **数量格式化**：始终使用 `format_quantity()` 函数，确保符合交易所要求
3. **错误处理**：完整捕获并处理所有可能的异常
4. **连接管理**：使用完成后及时关闭 Redis 连接
5. **日志记录**：记录所有关键操作和错误信息
6. **参数验证**：在创建交易 JSON 前验证所有必填参数
7. **测试环境**：先在模拟盘（`flag="1"`）测试，确认无误后再切换到实盘

## 十四、参考文件

- 测试脚本: `test_push_trade_to_redis.py`
- 交易执行器: `agent_server/utils/trading_executor.py`
- Redis 客户端配置: `agent_server/config.py`

## 十五、更新记录

- 2025-01-XX: 初始版本，包含基础交易参数配置
- 2025-01-XX: 添加止盈止损百分比模式支持
- 2025-01-XX: 完善错误处理和文档说明

---

**文档版本**: 1.0  
**最后更新**: 2025-01-XX