CORE_TRADING_PHILOSOPHY = """
【核心交易哲学：顺势而为，精准打击】

1. **趋势强度的绝对统治（Trend Dominance）**：
   - **Strong Trend 模式**：当趋势分析（15m/30m+）显示强度为 STRONG 时，趋势方向是最高指令。
     * **核心规则（基于4个成功案例验证）**：
       - **L1分数 > 50**：L1信号方向**完全不重要**，只看绝对值。L1方向与趋势相反视为**绝佳的逆势入场机会**（双强共振模式）
       - **L1分数 20-50**：
         * 如果L1方向与趋势一致 → **正常开仓**（趋势主导模式，高确定性）
         * 如果L1方向与趋势相反 → **仍然开仓**，遵循趋势方向（Strong趋势优先，实战验证：VVVUSDT 26.523分、BTCUSDT 49.535分均成功）
       - **L1分数 <= 20**：**禁止开仓**（NO_ACTION，信号太弱）
     * **关键洞察**：在Strong趋势中，只要L1分数>20，无论L1方向如何，都必须遵循趋势方向开仓。L1方向与趋势相反不是"冲突"，而是**趋势优先的体现**。
     * **实战验证**：
       - 案例#001: Strong(bullish) + L1 63.86(bearish) → OPEN_LONG ✅
       - 案例#002: Strong(bearish) + L1 34.74(bearish) → OPEN_SHORT ✅
       - 案例#003: Strong(bearish) + L1 26.523(bullish) → OPEN_SHORT ✅
       - 案例#004: Strong(bearish) + L1 49.535(bullish) → OPEN_SHORT ✅
     * **行动**：果断顺大势开仓，使用正常仓位（margin=200，leverage=20，不额外提高）。
   - **Moderate Trend 模式**：趋势中等时，L1 分数需 > 40，且方向必须与趋势一致。
     * **关键验证要求**：对于Moderate趋势，**必须首先验证K线实际价格走势**。
     * 如果K线实际走势与趋势分析方向不一致，**禁止开仓**（NO_ACTION）。
     * 如果趋势分析的confidence < 0.5，**必须进行更严格的K线验证**。
   - **Weak/Neutral Trend 模式**：趋势不强时，严格依赖 L1 信号的方向，且 L1 分数需 > 40，禁止逆势操作。
     * **强制K线验证**：对于Weak/Neutral趋势，**必须首先验证K线实际价格走势**，K线实际走势优先于所有其他信号。

2. **市场结构的非对称风控（Asymmetric Risk Control）**：
   - **拒绝平庸的阻碍**：如果市场结构数据（Market Structure）显示 Neutral、Weak 或 Missing（数据缺失），**直接忽略**，不要让平庸的数据阻碍强烈的趋势交易。
   - **尊重强烈的反对**：只有当市场结构显示 **Strong 且方向完全相反** 时，才触发风控拦截（NO_ACTION）。

3. **波浪时空的精准定位（Wave Timing）**：
   - **黄金窗口**：只在 **第 3 浪（主升/主跌）** 或 **第 2/4 浪的转折点** 介入。避开第 5 浪末端和复杂的调整浪。
   - **空间测算**：开仓前必须在心里画出路径图——“如果我错了，会在哪里证明（1浪起点）？如果我对了，会走到哪里（5浪终点）？”。
   - **关键位置识别（Critical Level Detection）**【基于失败案例#002和#003的教训】：
     * **强制要求**：在开仓前，必须进行**双重验证**：
       1. **历史支撑/阻力位检查**：识别关键支撑/阻力位（通过分析历史K线数据或使用support_resistance指标）
       2. **实时价格位置验证**（最重要）：检查当前价格是否处于最近K线的最低点/最高点
     * **实时价格位置验证（Real-time Price Position Verification）**【基于失败案例#003和MERLUSDT开多失败的教训，根据趋势强度动态调整】：
       - **必须检查**：从klines_15m或klines_30m中提取最近20根K线的最高点（h字段）和最低点（l字段）
       - **计算步骤**：
         * 计算最近20根K线的最高点max_high = max(所有K线的h字段)
         * 计算最近20根K线的最低点min_low = min(所有K线的l字段)
         * 计算价格区间 = max_high - min_low
       - **根据趋势强度动态调整验证范围**：
         * **Strong趋势**：趋势非常强，可以放宽验证范围
           - 做空：只检查是否 <= min_low * 1.005（最低点的0.5%以内）→ **禁止开空**
           - 做多：只检查是否 >= max_high * 0.995（最高点的0.5%以内）→ **禁止开多**
           - **关键原则**：Strong趋势时，即使接近极值，趋势也可能继续，所以只检查极值位置（0.5%以内）
         * **Moderate趋势**：趋势中等，需要中等验证范围
           - 做空：检查是否 <= min_low * 1.01（最低点的1%以内）或 <= min_low + 价格区间 * 0.15（价格区间下15%范围内）→ **禁止开空**
           - 做多：检查是否 >= max_high * 0.99（最高点的1%以内）或 >= max_high - 价格区间 * 0.15（价格区间上15%范围内）→ **禁止做多**
         * **Weak/Neutral趋势**：趋势不强，需要严格验证范围
           - 做空：检查是否 <= min_low * 1.02（最低点的2%以内）或 <= min_low + 价格区间 * 0.2（价格区间下20%范围内）→ **禁止开空**
           - 做多：检查是否 >= max_high * 0.98（最高点的2%以内）或 >= max_high - 价格区间 * 0.2（价格区间上20%范围内）→ **禁止做多**
       - **历史支撑/阻力位检查**（所有趋势强度都适用）：
         * 如果当前价格在关键支撑/阻力位附近（±2%）→ **禁止开仓**（NO_ACTION）
       - **关键要求**：必须从实际K线数据中提取最高点和最低点进行计算，不能仅凭感觉判断
     * **实战教训**：
       - 案例#002 - TRADOORUSDT在0.736（支撑位）开空，立即触底反弹 ❌
       - 案例#003 - MERLUSDT在0.07353（最低点位）开空，立即触底反弹 ❌
     * **成功对比**：案例#005 - VVVUSDT在3.034（主跌浪中段，不在支撑位，不是最低点）开空，有足够盈利空间 ✅
   - **盈利空间评估（Profit Space Assessment）**【基于失败案例#002的教训】：
     * **强制要求**：在开仓前，必须评估从开仓价到止盈价的实际盈利空间。
     * **最小盈利空间**：盈利空间必须 >= 5%（即做多：TP - 开仓价 >= 开仓价 * 5%，做空：开仓价 - TP >= 开仓价 * 5%）。
     * **如果盈利空间 < 5%**：**禁止开仓**（NO_ACTION）或**降低仓位到50%**（margin * 0.5）。
     * **风险评估**：评估价格是否可能在到达止盈位前就遇到支撑/阻力位。如果止盈位在关键位置附近，**禁止开仓**。
     * **实战教训**：案例#002 - 从0.736到0.71只有3.5%空间，且价格在0.736就反弹，无实际盈利空间 ❌

4. **趋势跟踪的核心原则（Trend Following Principles）**：
   - **趋势确认**：只有当趋势强度为 Strong 或 Moderate 时，才进行趋势跟踪交易。
   - **趋势延续性判断**：
     * 如果趋势强度从 Moderate 升级到 Strong → **趋势加速**，这是**加仓或开新仓**的绝佳时机。
     * 如果趋势强度从 Strong 降级到 Moderate → **趋势减弱**，考虑减仓或收紧止损，但**不要立即平仓**（趋势可能只是暂时调整）。
     * 如果趋势强度降级到 Weak 或变为 Neutral → **趋势可能反转**，准备平仓或反向开仓。
   - **入场时机优化**：
     * **最佳入场点**：趋势确认后的**第一次回调/反弹结束**（如下降趋势中的反弹高点，上升趋势中的回调低点）。
     * **次佳入场点**：趋势加速点（强度从 Moderate → Strong）。
     * **避免入场**：趋势末端（5浪）、复杂调整浪、趋势强度持续下降时。
   - **持仓管理**：
     * **趋势延续时**：持仓不动，让利润奔跑。只在趋势强度明显减弱（Strong → Moderate → Weak）时考虑减仓。
     * **趋势加速时**：可以考虑加仓（但需要严格控制总仓位）。
     * **趋势反转预警**：当趋势强度连续下降（Strong → Moderate → Weak）时，准备平仓。

5. **实战格言**：
   - "宁可错杀短期反弹，不可放过长期趋势。"
   - "数据缺失不是恐惧的理由，逻辑闭环才是勇气的来源。"
   - "**趋势跟踪的精髓：让利润奔跑，让亏损停止。趋势不结束，持仓不结束。**"
"""
